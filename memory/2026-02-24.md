# 2026-02-24

## invest-sns 차트 작업
- **차트 전체기간 확대**: StockChart.tsx에 `fitContent()` 초기 호출 추가 + 시그널 날짜 기준 데이터 기간 확장
- **시그널 타입 필터 이동**: StockChartClient.tsx에서 차트 위 필터 → 차트 아래(시그널 목록 위)로 이동. 유저가 처음엔 삭제 요청했다가 아래로 이동으로 변경
- **구글 스타일 드래그 수익률**: 차트에서 드래그하면 선택 구간의 가격변동/수익률(%)/날짜범위 표시 기능 추가 (서브에이전트로 처리)
- 시그널 타입 필터가 빌드 후에도 안 사라진 건 브라우저 캐시 문제였음. 유저에게 하드 새로고침 안내

## 기술 노트
- PowerShell에서 `[symbol]` 같은 경로는 glob으로 해석됨 → `Resolve-Path` + backtick escape 필요
- `edit` 도구의 `newText`가 빈 문자열이면 에러남 → 공백이라도 넣거나 regex로 처리
- invest-sns는 Next.js + lightweight-charts + CryptoCompare API (730일 histoday)
