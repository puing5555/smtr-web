# 2026-03-02

## 세상학개론 분석
- 98개 영상 중 25개 완료 (batch 1-5), 나머지 73개 분석 진행
- batch 6 완료 (4개 시그널: 비트코인, 이더리움 클래식 등)
- API 타임아웃 문제로 서브에이전트 2번 재시도
- 차트 수정 완료: 바 차트 → 라인 차트 + 목표주가 점선 + 시그널 점 (StockAnalystChart.tsx, next/dynamic SSR:false)
- 분석 백그라운드 계속 진행 중

## 수익률 "-" 버그 수정
- **원인**: `public/signal_prices.json` 43개 vs `data/signal_prices.json` 112개 — public이 오래됨
- **수정**: data → public 복사 (112개). 빌드+배포 필요
- 프론트엔드는 `/invest-sns/signal_prices.json` fetch (public/ 경로)

## 애널리스트 탭 전면 수정 (서브에이전트 진행 중)
1. 증권사별 목표가 요약 카드 삭제
2. 차트 시그널 점 위치 수정 (주가 라인 위에 정확히)
3. PDF 567건 다운로드 + 텍스트 추출 + AI 한줄요약
4. 모달 상세요약 (투자포인트/실적/밸류에이션/리스크/결론)
5. 애널리스트명 PDF에서 추출
- 서브에이전트: agent:main:subagent:edaa7a1d-8429-4d73-8fa1-4901b502ffb7 (타임아웃 1시간)

## DB 테이블 구조 (중요!)
- **시그널 테이블**: `influencer_signals` (NOT `signals` — signals는 빈 테이블)
- 현재 97개 시그널 in influencer_signals
- 컬럼: id(UUID), video_id(UUID FK), speaker_id(UUID FK), stock, ticker, market, mention_type, signal, confidence, timestamp, key_quote, reasoning, review_status, pipeline_version
- **analyst_reports**: 현재 0개 (PDF 567건 배치 처리 중)
- **speakers**: 23개
- Service Role Key 사용해야 데이터 조회/삽입 가능 (anon key로는 RLS 때문에 안 됨)

## 세상학개론 시그널 업로드 (진행 중)
- 87개 시그널 준비 (sesang101_supabase_upload.json)
- influencer_signals 테이블에 INSERT 중 (서브에이전트)
- video_id, speaker_id UUID 매핑 필요

## PDF 567건 배치 처리 (진행 중)
- scripts/pdf_batch_processor.py 백그라운드 실행 중
- Claude API로 AI 요약 생성 (한줄 + 상세)
- 애널리스트명 PDF에서 추출
- analyst_reports 테이블에 ai_summary, ai_detail, analyst_name 업데이트 예정

## 프로젝트 구조 메모
- Next.js App Router (app/ 디렉토리, pages/ 없음)
- 종목 페이지: app/stock/[code]/StockDetailClient.tsx
- 프론트엔드 signal_prices.json: public/ 폴더에서 fetch → data/와 반드시 동기화
- Supabase RLS로 anon key 직접 조회 안 됨
