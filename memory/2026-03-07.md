# 2026-03-07 작업 로그

## 오전 세션 (07:50 ~ 08:42 GMT+7)

### 1. 대시보드 리뉴얼
- **파일**: `app/dashboard/page.tsx` 전면 재작성
- **변경**: 탭 삭제(전체/내종목/시그널), 4섹션 구조로 변경
  - ① 오늘 알림 (클릭→종목 페이지, 공시는 ?tab=disclosure)
  - ② 보유종목/관심종목 탭 (클릭→/stock/[ticker])
  - ③ 시황 (미국장/국내장/원자재코인/Fear&Greed/핵심이슈)
  - ④ 오늘의 인사이트 (AI)
- "어젯밤 미국장", "어젯밤 내 종목" 섹션 삭제

### 2. 인플루언서 프로필 404 수정
- **파일**: `lib/speakerSlugs.ts`
- **원인**: `koreanToSlug()`이 hash 기반 비가역 인코딩 사용 → `slugToSpeaker()` 역변환 불가
- **수정**: Unicode hex 가역 인코딩으로 변경 (`u` + 4자리 hex)
  - '신현철' → `uc2e0d604cc60` → 역복원 가능
- `slugToSpeaker()` 함수에 `koreanFromSlug()` fallback 추가

### 3. 타임스탬프 추출 근본 수정
- **파일**: `scripts/subtitle_extractor.py`, `scripts/signal_analyzer*.py`
- **원인**: `TextFormatter` 사용 → 자막 타임스탬프 전부 소실 → Claude 추측 → 0:00
- **수정**:
  - `format_with_timestamps()` 함수 추가: `[MM:SS] 텍스트` 형태로 변환
  - `parse_vtt_content()` 전면 재작성: VTT `-->` 라인에서 시간 파싱 후 보존
  - `signal_analyzer*.py`: "자막의 [MM:SS] 태그에서 직접 읽어라, 추측 금지" 지시 추가
  - `prompts/pipeline_v10.md`: timestamp 규칙 강화

### 4. V11 종목명 정규화 로직
- **파일**: `scripts/stock_normalizer.py` (신규 생성)
- **기능**: Claude 추출 직후 표준명으로 자동 변환
  - "엔비디아 (NVDA)" → "엔비디아"
  - "버크셀" → "버크셔해서웨이"
  - "알파벳" → "구글"
  - ticker 298040 → 267260 자동 수정
- **연결**: `signal_analyzer_rest.py`의 `parse_analysis_response()` 직후 호출

### 5. 종목명 중복 20그룹 DB 정규화
- **방법**: DEV 서브에이전트 (DEV-stock-name-normalize, 5분, 41K tokens)
- **결과**: 804개 레코드, 20그룹 정리 완료
  - NVDA: "엔비디아 (NVDA)" → "엔비디아" (1건)
  - GOOGL: "알파벳", "구글(알파벳)" 등 6가지 → "구글" (6건)
  - BRK.A: "버크셀 해서웨이" → "버크셔 해서웨이" (1건 오타)
  - STZ: "컨스텔레이션 브랜드" → "브랜즈" (오타)
  - ticker 298040 현대일렉트릭 → 267260으로 수정
  - 등 총 59건 UPDATE, 실패 0건

### 6. 배포 방식 수정 (중요)
- **문제**: `git push origin gh-pages`로 소스코드 전체가 올라가는 사고 반복 (3회 발생)
- **원인**: 서브에이전트들이 잘못된 방법으로 배포
- **수정**: 
  - `deploy.ps1` 생성: `out/` 폴더에서 git init → push하는 올바른 배포 스크립트
  - `DEPLOYMENT.md`: ❌ 금지 / ✅ 올바른 방법 명시
- **올바른 배포 명령**:
  ```powershell
  cd out; git init; git checkout -b gh-pages; git add .; git commit -m "deploy: ..."; git remote add origin https://github.com/puing5555/invest-sns.git; git push origin gh-pages --force; cd ..; Remove-Item -Recurse -Force out\.git
  ```

### 7. /portfolio 관리 페이지
- **파일**: `app/portfolio/page.tsx` (DEV 서브에이전트 생성)
- **기능**: 보유종목/관심종목/매도기록 3탭 CRUD
- **연결**: 사이드바 + 대시보드 "관리 →" 링크

### 8. 사이드바 최종 정리
- **삭제**: 보유종목, 관심종목, 내 종목, 메모
- **최종 메뉴 8개**: 대시보드 / 피드 / 탐색 / 공시 / 포트폴리오 / 캘린더 / 알림 / 프로필
- BottomNav (모바일): 대시보드 / 탐색 / 포트폴리오 / 알림 / 프로필

### 9. 채널 확장 Top 20 리스트 JAY에게 전달
- 승인 대기 중 (채널 크롤링 시작 여부 미결)

## 현재 사이트 상태
- URL: https://puing5555.github.io/invest-sns/dashboard
- 정상 운영 중

## 미완료 작업
- 기존 DB 타임스탬프 잘못된 것들 재처리 (나중에)
- 채널 확장 Top 20 승인 후 크롤링 시작
- ai_detail 519건 재생성 (미실행)
- Supabase research_invest_platforms 테이블 생성
