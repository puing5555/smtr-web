# 2026-02-28 작업 로그

## 인플루언서 프로필 페이지 대규모 개선 (21:08~22:35)

### 완료된 UI 작업
1. **프로필 헤더 개선**
   - 관심 종목 표시: 발언 많은 순, 한글명만 (티커 제거) → `SK하이닉스(2) · SOXX(1)`
   - 총 시그널 수 표시

2. **종목 필터 탭 추가**
   - `전체` + 종목별 탭 (발언 많은 순)
   - 탭 클릭 시 해당 종목만 필터링
   - 종목명 (티커) 형태로 띄어쓰기 포함: `SK하이닉스 (000660)`

3. **SignalCard → 테이블 변환**
   - 컬럼: 날짜 | 종목 | 신호 | 핵심발언 | 영상링크
   - 행 클릭 → SignalDetailModal 열림

4. **종목 상세 인플루언서 탭 테이블에서 "인플루언서" 컬럼 제거**
   - StockDetailClient.tsx, StockDetailClientV2.tsx 모두 수정

5. **종목 한글명 매핑 시스템**
   - `lib/stockNames.ts` 신규 생성
   - `formatStockDisplay()`: 한글명 (티커) 형태
   - `formatStockShort()`: 관심종목용 한글명만
   - 매핑: CC→캔톤네트워크, BTC→비트코인, XRP→리플, NVDA→엔비디아 등

6. **날짜에 년도 포함** (year: 'numeric' 추가)

### 커밋 이력
- `인플루언서 프로필: 관심종목 표시 + 종목 필터탭 추가`
- `시그널카드→테이블 변환 + 인플루언서 컬럼 제거`
- `종목 한글명(티커) 표시 + 테이블행 클릭→모달 + 모달 채널명 표시`
- `테이블 날짜에 년도 포함`
- `관심종목 한글명만 + 종목 띄어쓰기 추가`

## 자막 추출 파이프라인 현황 (22:24~22:35)

### 이미 완료된 것
- **자막 추출 완료:** 15개 (C:\Users\Mario\work\subs)
  - 부읽남TV 9개 (booread_*)
  - 이효석 3개 (hyoseok_*) — key가 "subtitles"임 주의
  - 슈카 3개 (syuka_*)
- **V9 분석+DB INSERT 완료:** 전부. DB에 41개 영상, 74개 시그널
- pipeline_v9_results.json에 12개 분석 결과 기록

### 미추출 7개 (YouTube IP 차단)
- 슈카: ksA4IT452_4, nm5zQxZSkbk, XveVkr3JHs4, N7xO-UWCM5w
- 이효석: B5owNUs_DFw, bmXgryWXNrw
- 달란트: 5mvn3PfKf9Y

### 시도한 방법 (전부 실패)
1. youtube-transcript-api → IpBlocked
2. yt-dlp → 429 Too Many Requests
3. Selenium → 페이지 로드 OK but timedtext API 429
4. Selenium JS fetch → 빈 응답
5. Selenium navigate to caption URL → 빈 응답
6. Invidious 프록시 (inv.nadeko.net 등) → 목록 OK, 내용 빈 응답

### 결론
- YouTube timedtext API가 이 IP에서 완전 차단
- VPN 또는 다른 네트워크 필요
- 유저에게 VPN 켜달라고 요청함

## DB 참고
- 코린이 아빠 시그널: published_at이 전부 NULL → created_at(2026-02-27)으로 표시됨
- stock 필드: 한글(삼성전자)과 영문티커(CC, BTC) 혼재
- stock_name 컬럼은 존재하지 않음 → 프론트 매핑으로 해결

## 기술 메모
- `subs/` 자막 파일 key 불일치: 일부는 "segments", 일부는 "subtitles"
- pipeline_v9.py의 SUBS_DIR = C:\Users\Mario\work\subs (invest-sns/subs가 아님)
- Selenium: webdriver_manager 사용, headless Chrome
