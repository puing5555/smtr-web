# 2026-03-01 작업 로그

## V10.1 DB 수정 (서브에이전트)
- 김장년→김장열 화자명 4건 수정 (새 speaker 레코드 생성, 시그널 speaker_id 변경)
- 배재규 반도체 바스켓 중복 2건 통합 삭제
- 시그널 101→99개

## V10.3 프롬프트 자율 개선 (서브에이전트)
- V10.1→V10.3 3라운드 자율 개선
- 99개 시그널에서 144건 이슈 발견 (key_quote 113건)
- 7단계 검증 체크리스트, 절대금지 패턴 확장
- 결과: prompts/pipeline_v10.md (V10.3)

## 자동화 파이프라인 스크립트 (서브에이전트)
- scripts/ 폴더에 6개 모듈 완성 (auto_pipeline.py 등)
- sesang101(세상의모든지식) 채널 테스트: 10개 중 8개 통과(80%)

## Admin 신고관리 버그 수정 3건
1. **필드명 수정**: admin page 수정안 비교 UI에서 `original.quote`→`original.key_quote`, `original.analysis_reasoning`→`original.reasoning`
2. **재배포**: 이전 변경들이 배포 안 돼 있었음 → 빌드+gh-pages 배포
3. **AI 검토 쿼리 에러**: `handleAiReview`의 Supabase select에서 `analysis_reasoning`(존재하지 않는 컬럼) → `reasoning`으로 수정. 이게 "신고 데이터를 찾을 수 없습니다" 에러의 원인이었음

## 핵심 교훈
- **DB 컬럼명 불일치 주의**: `influencer_signals` 테이블은 `reasoning`과 `key_quote` 사용. `analysis_reasoning`이나 `quote`는 존재하지 않음
- admin page에 아직 `analysis_reasoning` 참조 남아있을 수 있음 (AI 수정안 JSON 파싱 부분은 AI가 생성하는 JSON 필드명이라 OK)
- speakers 테이블에 "김장년" 레코드가 아직 존재 (orphan) — 정리 필요

## 종목 페이지 일괄 생성 + Edge Function 전환 (서브에이전트 15:38~15:44)
- `generateStaticParams`: 하드코딩 5개 → Supabase에서 동적 조회 28개 종목
- `StockDetailClient`: nameMap 8개→22개 + Supabase stock 컬럼 우선 사용
- `lib/anthropicClient.ts`: corsproxy.io 제거 → Supabase Edge Function 프록시
- `supabase/functions/anthropic-proxy/index.ts` 생성 + 배포 완료
- Supabase access token 생성: `openclaw-deploy` (만료 없음, 브라우저로 생성)
- ANTHROPIC_API_KEY를 Edge Function 환경변수로 설정 완료
- /admin AI 검토 시 API 키 입력 불필요해짐
- Git 커밋 `886c315` + push 완료

## /admin AI 응답 처리 전면 수정 (16:00~16:18)
- `parseAiJson()` 유틸 함수 도입: 모든 AI 응답 JSON 파싱 통일 (백틱/마크다운 자동 제거)
- 컬럼명 완전 통일: `quote`→`key_quote`, `analysis_reasoning`→`reasoning` (프롬프트+DB+UI 전체)
- AI 프롬프트에 "순수 JSON만 반환, markdown 금지" 지시 추가
- signal_reports 테이블에 `updated_at` 컬럼 없음 → update 쿼리에서 제거
- 신고관리 AI검토 결과 표시: raw markdown → "✅ 문제없음" / "⚠️ 수정필요" 요약
- 프롬프트 개선안에 ai_review 결과 데이터 포함하도록 수정

## 중복 시그널 전수 검사 + 정리 (16:18)
- video_id + speaker_id + stock + signal 조합 기준 9쌍 중복 발견
- 각 쌍에서 key_quote 짧은(덜 완전한) 쪽 삭제
- 삭제 시 FK 충돌: signal_reports 1건 + signal_votes/memos 먼저 삭제 후 시그널 삭제
- service_role key 필요 (anon key로는 DELETE 안 됨 - RLS)
- **시그널 99개 → 90개**

## Git 커밋
- `admin 신고관리 필드명 수정 + 재배포`
- `AI 검토 쿼리 analysis_reasoning→reasoning 컬럼명 수정`
- `종목 페이지 일괄 생성 + Edge Function 전환` (`886c315`)
- `admin AI검토 updated_at 컬럼 제거`
- `admin 테이블 UI 수정: AI검토 line-clamp-3, 종목명 nowrap, 컬럼너비 고정`
- `AI 수정안 JSON 백틱 제거 + 컬럼명 수정`
- `admin 목록 AI검토/수정안 요약 표시`
- `admin AI검토 표시 강화 + 프롬프트개선안 ai_review 기반`
- `admin AI응답 JSON파싱 통일 + 컬럼명 통일 + 프롬프트 JSON전용 지시`
