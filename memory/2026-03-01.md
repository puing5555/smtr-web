# 2026-03-01 작업 로그

## 완료된 작업
1. **인플루언서 카드 프로필 수정** — 게스트(채널명≠발언자)는 이니셜, 호스트만 유튜브 썸네일. `isHost` 로직: channelList에 speaker.name 포함 여부 체크
2. **멤버십 영상 삭제** — 코린이아빠 11영상 + 11시그널 DB DELETE (84→73개). 전부 캔톤네트워크 회원전용 콘텐츠
3. **파이프라인 멤버십 제외 규칙** — pipeline_v9.py에 "멤버십/회원전용 영상 = 분석 제외" 추가
4. **리포트 탭 삭제** — 종목 상세페이지에서 리포트 탭 제거 (애널리스트와 중복). 탭: 피드|인플루언서|애널리스트|공시|실적|임원매매|일정|메모
5. **좋아요 하트 중복 버그** — SignalDetailModal 상단바의 별도 ❤️ likeCount 표시 제거, 하트 버튼(♡/❤️)에 숫자 통합
6. **Supabase SQL 실행** — 브라우저로 SQL Editor에서 직접 실행: signal_memos 테이블 생성 + signal_reports에 ai_review, ai_suggestion 컬럼 추가 (signal_votes는 이미 존재)
7. **서브에이전트: 추가 시그널 파이프라인** — IP 차단으로 새 자막 추출 실패, 기존 자막에서 1개만 추가 (73→74)
8. **서브에이전트: 영상 요약 생성** — subs/ 폴더 fulltext 파일 30개+로 진짜 요약 생성 중 (진행 중)

## 시그널 현황
- 현재: **74개** (목표 100개)
- 채널별: 삼프로TV 20 + 슈카 17 + 이효석 9 + 부읽남 7 + 달란트 4 + 기타
- 코린이아빠 멤버십 11개 전부 삭제됨

## 자막 파일 위치
- **C:\Users\Mario\work\subs\** — fulltext .txt 파일 30개+ (booread, sampro, hyoseok, syuka, dalant, korini 접두사)
- invest-sns/subs/ 폴더에는 스크립트/JSON만 (실제 자막 아님)

## 기술 메모
- yt-dlp 미설치 (sandbox 환경)
- YouTube IP 차단 상태 — Selenium DOM 방식(16배속 CC 캡처)이 유일한 자막 추출 방법
- Supabase 브라우저 조작: Monaco editor에 `window.monaco.editor.getModels()[0].setValue()` 사용, Run 버튼은 `querySelectorAll('button').find(b => b.textContent.includes('Run'))` 

## Git 커밋
- `인플루언서 카드 프로필: 게스트는 이니셜, 호스트만 유튜브 썸네일`
- `멤버십 영상 11개 삭제 + 파이프라인 멤버십 제외 규칙 추가 (84→73 시그널)`
- `리포트 탭 삭제 + 좋아요 하트 중복 버그 수정`
